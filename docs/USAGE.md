# 使用指南

## 快速开始

### 1. 安装

#### 环境要求
- Python 3.10+
- MySQL 5.7+
- Windows 10/11

#### 安装步骤

```bash
# 1. 克隆项目
git clone https://github.com/yourusername/translearn.git
cd translearn

# 2. 安装依赖
pip install -r requirements.txt

# 3. 创建数据库
mysql -u root -p < scripts/create_database.sql

# 4. 配置应用
# 复制 data/config.example.toml 到 data/config.toml
# 编辑配置文件，设置数据库连接和 API Key

# 5. 初始化数据库
python scripts/init_database.py

# 6. 运行应用
python -m src
```

### 2. 配置

#### 数据库配置

编辑 `data/config.toml`:

```toml
[database]
host = "localhost"
port = 3306
user = "root"
password = "your_password"
database = "translearn"
```

#### AI 翻译配置

```toml
[translation.ai]
provider = "dashscope"  # 或 "openai"
model = "qwen-turbo"
api_key = "your_api_key"
```

支持的 AI 提供商:
- **DashScope**: 阿里云通义千问，国内访问快
- **OpenAI**: GPT 系列，需要 VPN

#### 快捷键配置

```toml
[hotkey]
translate = "ctrl+q"              # 翻译快捷键
screenshot_ocr = "ctrl+shift+q"   # OCR 快捷键
toggle_monitor = "ctrl+shift+t"   # 切换剪贴板监听
open_main_window = "ctrl+shift+l" # 打开主窗口
```

## 功能使用

### 翻译功能

#### 方式一：热键取词（推荐）

1. 在任意应用中选中文本
2. 按 `Ctrl+Q`
3. 在鼠标附近弹出翻译窗口
4. 查看翻译结果

优点:
- 不打断工作流
- 不污染剪贴板
- 支持所有应用

#### 方式二：剪贴板监听

1. 启用剪贴板监听（按 `Ctrl+Shift+T`）
2. 复制文本 (`Ctrl+C`)
3. 自动弹出翻译窗口

优点:
- 零学习成本
- 适合大量复制场景

#### 方式三：OCR 截图翻译

1. 按 `Ctrl+Shift+Q`
2. 框选包含文字的区域
3. 自动识别并翻译

优点:
- 支持图片中的文字
- 支持 PDF、视频字幕

### 词库管理

#### 查看词库

1. 按 `Ctrl+Shift+L` 打开主窗口
2. 点击"📚 词库"标签
3. 浏览所有收藏的词条

#### 搜索词条

在搜索框输入关键词，即可搜索原文或翻译。

#### 收藏词条

在翻译窗口点击"⭐ 收藏"按钮。

### 学习复习

（开发中）

基于 SM-2 算法的智能复习系统，帮助记忆单词。

### 统计分析

查看学习数据:
- 每日翻译次数
- 词库增长
- 学习时长
- AI 调用统计

## 高级功能

### 黑名单

某些应用（如密码管理器）不应该被监听:

```toml
[blacklist]
apps = ["PasswordManager", "Bitwarden", "KeePass"]
```

### 自动保存

重复查询 3 次以上自动保存到词库:

```toml
[features]
auto_save = true
auto_save_threshold = 3
```

### 缓存管理

翻译结果会缓存 30 天:

```toml
[cache]
enabled = true
expire_days = 30
max_size_mb = 100
```

## 常见问题

### 1. 热键不生效？

- 检查是否与其他软件快捷键冲突
- 尝试修改配置文件中的快捷键
- 以管理员身份运行

### 2. 数据库连接失败？

- 确认 MySQL 服务已启动
- 检查配置文件中的数据库信息
- 确认数据库已创建

### 3. 翻译失败？

- 检查网络连接
- 确认 API Key 配置正确
- 查看日志: `data/logs/translearn_*.log`

### 4. 悬浮窗不显示？

- 检查是否有其他窗口遮挡
- 尝试调整窗口位置配置
- 查看日志排查错误

## 技巧与建议

1. **合理使用翻译器**: 单词用词典，句子用 AI，节省成本
2. **定期复习**: 利用复习功能巩固记忆
3. **标签分类**: 给词条打标签，方便管理
4. **定期备份**: 导出词库数据防止丢失
5. **黑名单设置**: 避免敏感应用被监听

## 快捷键速查

| 快捷键 | 功能 |
|-------|------|
| `Ctrl+Q` | 翻译选中文本 |
| `Ctrl+Shift+Q` | OCR 截图翻译 |
| `Ctrl+Shift+T` | 切换剪贴板监听 |
| `Ctrl+Shift+L` | 打开主窗口 |
| `Esc` | 关闭悬浮窗 |

## 更新日志

见 [CHANGELOG.md](CHANGELOG.md)

